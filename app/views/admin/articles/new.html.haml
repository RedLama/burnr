#article_new
  .panel.panel-default
    .panel-heading
      %h3.panel-title= t('.add_title')
    .panel-body
      = form_for [:admin, @article] do |f|
        = render partial: "form", locals: {builder: f}
        = f.submit t('.submit'), class: "btn btn-default"
